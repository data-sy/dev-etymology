// Config.sample.xcconfig
// 복사본을 만든 뒤 파일명을 Config.xcconfig 로 바꾸세요.
// Config.xcconfig 는 .gitignore 에 의해 커밋에서 제외됩니다.
//
// 앱은 더 이상 API 키가 필요 없습니다. Claude 호출은 백엔드 프록시(devetym-proxy)를
// 경유하며, 키는 프록시 서버 시크릿에만 존재합니다.
// 이 파일은 Xcode 빌드 설정의 baseConfiguration 자리만 채웁니다(현재 빈 설정).
