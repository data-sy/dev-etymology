# DevEtym PRD v1.0

> **성격: 제품 기획 원본(historical baseline).** 2026-04-07 작성된 MVP 1.0 PRD를 Notion에서 이관한 것이다. 제품의 *왜*(문제 정의·타겟·유저 스토리·콘텐츠 전략)는 지금도 유효하므로 정본으로 둔다.
> **현재 동작/상태 정본은 따로 있다** — 구현 명세는 [`../specs/spec.md`](../specs/spec.md), 진행/완료 상태는 [`../../ROADMAP.md`](../../ROADMAP.md), 의사결정은 [`../adr/`](../adr/). 충돌 시 그쪽을 신뢰할 것.

## 현재와 달라진 점 (2026-06 기준 — 이 PRD 작성 후 변경된 사실)

원본을 그대로 두되, 출시 시점 실제와 어긋난 항목만 여기서 교정한다. 본문에서 아래 항목이 나오면 이 표를 우선한다.

| 본문 서술 (2026-04) | 현재 사실 (2026-06) |
|---|---|
| 번들 ID `com.robin.devdict` (결정 사항 표) | **`com.robin.devetym`** — 본문 다른 곳 표기가 맞고, 결정 표가 오타 |
| 큐레이션 DB "약 200~300개" | **650개 최종** 번들 (ROADMAP Done) |
| AI 연동 = Claude API 키를 앱에 두고 직접 호출 (`Info.plist`의 `CLAUDE_API_KEY`) | **온디바이스 키 제거 → 백엔드 프록시 경유**, 기기당 일 10회 한도. 근거 [ADR-0001](../adr/0001-backend-proxy-hosting.md) (PR #26) |
| "검색 자동완성" → P2(제외) | **구현됨** (자동완성 동작 중) |
| "오프라인 전체 DB" 제외 | 번들 650개는 **오프라인 즉답**, AI 폴백만 온라인 |
| 검색창 위치(상단 가정) | **하단 재배치**(한 손 도달성) — 2026-06-22 |

---

# 제품 개요

- **앱 표시 이름**: 개발 어원 사전
- **번들 ID**: com.robin.devetym
- **리브랜딩 후보**: Rootly (추후 검토)
- **플랫폼**: iOS (SwiftUI)
- **버전**: MVP 1.0
- **작성일**: 2026-04-07

개발 용어의 어원과 작명 이유를 한국어로 설명하는 사전 앱. 단순히 뜻을 알려주는 게 아니라, 왜 그 이름이 붙었는지를 설명해서 개념 이해와 기억을 돕는다.

---

# 앱 이름 결정 배경

- **표시 이름**: 개발 어원 사전
- **번들 ID**: com.robin.devetym

표시 이름 "개발 어원 사전"은 한국어 타겟 사용자가 앱을 직관적으로 이해하기 위한 이름이다. 실제로 이 앱이 하는 일의 본질은 **Etymology(어원 연구)**이고, 번들 ID의 `etym`은 그 근본 개념을 반영한다.

이름 결정 과정:
- `DevEtym` → "Etymology"가 한국어 사용자에게 낯설어서 (표시 이름에서) 제외
- `개발 어원 사전` → 타겟에게 직관적, App Store 검색 친화적
- 번들 ID는 `devetym`으로 고정 — 나중에 표시 이름을 바꿔도 호환
- 리브랜딩 후보: `Rootly` (추후 검토)

---

# 문제 정의

개발 공부를 시작한 주니어 개발자와 직무 전환자는 영어 기술 용어가 낯설다. 용어의 뜻은 어딘가에서 찾을 수 있지만, **왜 그 이름인지**는 어디에도 없다. 이름의 유래를 모르면 비슷한 용어끼리 헷갈리고, 개념이 머릿속에 붙지 않는다.

---

# 타겟 유저

**주요 타겟**: 한국어를 사용하는 주니어 개발자 및 직무 전환자
- 강의나 문서를 보다가 용어가 나왔을 때 빠르게 검색하고 싶다
- 영어 용어가 낯설고, 어원을 알면 이해가 쉬워진다고 느끼는 사람
- 면접 준비 중 CS 용어 정리가 필요한 사람

---

# 유저 스토리

## US-01: 용어 검색
> 나는 **강의 중 모르는 용어가 나왔을 때**, **빠르게 검색해서 어원과 작명 이유를 확인하고 싶다**, **그래서 용어를 개념과 함께 이해할 수 있다**.

**인수 기준**
- 검색창에 영어 용어를 입력하면 결과가 즉시 표시된다
- 결과에는 어원, 작명 이유, 한 줄 요약이 포함된다
- DB에 없으면 AI가 생성한 설명을 보여준다 (폴백)
- DB에 있는 용어는 즉시 로드, AI 생성은 로딩 표시 후 결과 표시

## US-02: 검색 히스토리
> 나는 **예전에 찾아봤던 용어를 다시 까먹었을 때**, **히스토리에서 바로 찾아보고 싶다**, **그래서 다시 검색하는 수고를 덜 수 있다**.

**인수 기준**
- 검색한 용어는 자동으로 히스토리에 저장된다
- 히스토리는 최근 검색순으로 표시된다
- 히스토리 항목을 탭하면 바로 해당 용어 상세 화면으로 이동한다
- 개별 항목 삭제 및 전체 삭제가 가능하다

## US-03: 용어 북마크
> 나는 **나중에 다시 보고 싶은 용어를 저장하고 싶다**, **그래서 면접 준비나 복습 때 빠르게 모아볼 수 있다**.

**인수 기준**
- 상세 화면에서 북마크 버튼으로 저장/해제할 수 있다
- 북마크 목록은 별도 탭에서 확인할 수 있다
- 북마크는 앱을 닫아도 유지된다 (로컬 저장)

---

# 콘텐츠 구조 (용어 카드)

| 항목 | 설명 | 예시 (mutex) |
|---|---|---|
| **용어** | 검색한 영어 용어 | mutex |
| **한 줄 요약** | 핵심만 담은 한국어 한 문장 | 동시 접근을 막는 잠금 장치 |
| **어원** | 단어의 어원 분해 | mutual + exclusion → 상호 배제 |
| **작명 이유** | 왜 이 이름이 붙었는지 | 두 스레드가 '서로(mutual) 막는(exclusion)' 개념 |

---

# 콘텐츠 전략

**큐레이션 DB + AI 폴백 방식** (수치는 [현재와 달라진 점](#현재와-달라진-점-2026-06-기준--이-prd-작성-후-변경된-사실) 참고)
- 자주 검색될 핵심 용어를 배치로 사전 생성해 번들 DB로 제공
- DB에 없는 용어는 Claude로 실시간 생성
- AI 실시간 생성 결과는 캐시해서 이후 동일 검색 시 재사용 (비용 절감)

**콘텐츠 신뢰도 고지 방식**
- 모든 콘텐츠는 AI 생성 (배치 DB 포함, 사람 검수 없음)
- AI 생성 뱃지 사용 안 함 (모든 콘텐츠가 동일 조건이므로 구분 불필요)
- **온보딩 화면**에서 1회 고지: "이 앱의 모든 설명은 AI가 생성합니다. 오류가 있을 수 있으니 제보해 주세요."
- **결과 화면 하단**에 고정 링크: "오류 제보하기"

---

# 기능 목록 (MVP)

| 기능 | 우선순위 | 비고 |
|---|---|---|
| 용어 검색 | P0 | 핵심 기능 |
| 용어 상세 카드 | P0 | 어원 + 작명 이유 + 요약 |
| AI 폴백 (DB 미존재 용어) | P0 | Claude 연동 |
| 검색 히스토리 | P1 | 로컬 저장 |
| 북마크 | P1 | 로컬 저장 |
| 탭바 3탭 구조 | P1 | 검색 / 북마크 / 히스토리 |
| 온보딩 AI 고지 | P1 | 첫 실행 1회 표시 |
| 오류 제보하기 | P1 | 결과 화면 하단 고정, 이메일/폼 연결 |

---

# MVP 제외 항목

(일부는 이후 구현됨 — [현재와 달라진 점](#현재와-달라진-점-2026-06-기준--이-prd-작성-후-변경된-사실) 참고)

| 항목 | 제외 이유 |
|---|---|
| AI 생성 뱃지 | 모든 콘텐츠가 AI 생성 → 온보딩 고지로 대체 |
| 카테고리 태그 | DB 구축 시 태그 필드 함께 추가 → P2 |
| 떠오르는 용어 | 운영 부담, 홈에 자리만 예약 → P2 |
| 검색 자동완성 | (이후 구현됨) |
| 공유 기능 | 핵심 가치와 무관 → P2 |
| 퀴즈 / 학습 모드 | 범위 과다 → P2 |
| 다국어 지원 | 타겟이 한국어 사용자로 명확 |
| 오프라인 전체 DB | (번들 650개는 오프라인 즉답, AI 폴백만 온라인) |

---

# 화면 구성 (MVP)

탭바 3탭 (하단 고정). 결과 화면은 NavigationStack push (iOS 표준). 탭바는 결과 화면에서도 유지.

---

# 기술 스택

- **프레임워크**: SwiftUI
- **데이터 저장**: SwiftData (히스토리, 북마크, AI 캐시)
- **AI 연동**: Claude (폴백 생성용 — 현재는 백엔드 프록시 경유, [ADR-0001](../adr/0001-backend-proxy-hosting.md))
- **큐레이션 DB**: 앱 번들 내 JSON 파일 (`Resources/terms.json`)
- **네트워크**: URLSession

---

# 결정 사항 (확정, 2026-04 기준)

| 항목 | 결정 |
|---|---|
| 앱 표시 이름 | 개발 어원 사전 |
| 번들 ID | com.robin.devetym |
| 리브랜딩 후보 | Rootly (추후 검토) |
| AI 뱃지 | 사용 안 함 |
| AI 고지 방식 | 온보딩 1회 + 결과 화면 하단 제보 링크 병행 |
| 카테고리 태그 | P2 |
| 네비게이션 | NavigationStack push (iOS 표준) |
| 탭바 | 3탭 하단 고정, 결과 화면에서도 유지 |

---

# 미결 사항 (2026-04 작성 시점 — 대부분 이후 해소됨)

- 큐레이션 DB 초기 용어 수 확정 → 650개로 확정
- AI 생성 프롬프트 포맷 설계 → `docs/ai-quality/`·`docs/db-expand/`에서 진행
- 오류 제보하기 구현 방식 (이메일 링크 vs 외부 폼) → mailto 이메일 링크로 구현
