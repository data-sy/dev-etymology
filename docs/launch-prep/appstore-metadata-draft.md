# App Store 메타데이터 초안 (5인 라운드테이블 산출물)

> 생성: `appstore-metadata-prompt.md`를 실행한 결과. **상태 정본은 `ROADMAP.md`**, 스펙은 [`../launch-prep.md`](../launch-prep.md) B.
> 글자수는 한국어 기준 실측(공백·기호 포함). `[사람 결정]` 태그는 최종 선택/입력이 사람 몫.
> 제품 사실 확인일 기준: 번들 650개 · 일 한도 10(백엔드 enforce) · 지원 이메일 oddmuffinstudio@gmail.com · 수집은 **명시적 동의 시에만**(opt-in).

---

## 필드별 최종안

### 앱 이름 (name) — ≤ 30자
| 안 | 값 | 글자수 | 근거 |
|---|---|---|---|
| **A (권장)** | `개발 어원 사전` | 8 | 브랜드 정착·검색 시 정확 매칭. 단 키워드 흡수량 적음 |
| B | `개발 어원 사전: 코딩 용어 뜻` | 17 | "코딩·용어·뜻" 키워드를 이름에서 흡수(ASO 유리), 0.5초 이해도↑ |

`[사람 결정]` — 브랜드 단순함(A) vs 검색 노출(B). [ASO]는 B, [카피]는 A 선호 → 트레이드오프. 처음엔 A로 등록 후 노출 데이터 보고 B로 전환도 가능.

### 부제 (subtitle) — ≤ 30자
| 안 | 값 | 글자수 | 근거 |
|---|---|---|---|
| **A (권장)** | `프로그래밍 용어의 유래와 작명 이유` | 19 | 제품의 핵심 차별점(유래·작명)을 한 줄로. "프로그래밍/용어" 키워드 흡수 |
| B | `면접 준비 코딩 용어, 오프라인 즉답` | 20 | 타깃(면접 준비)·기능(오프라인) 직격. "코딩/면접" 키워드 |
| C | `650개 용어 오프라인 사전+AI 설명` | 21 | 규모·오프라인·AI를 모두 노출(기능 요약형) |

`[사람 결정]` — 이름 A를 쓰면 부제는 키워드 보강형(B/C)이 유리, 이름 B를 쓰면 차별점형(A)이 보완적. **추천 조합: 이름 A + 부제 A**(차별점 강조) 또는 **이름 B + 부제 B**(키워드 최대화).

### 키워드 (keywords) — ≤ 100자, 쉼표구분, 쉼표 뒤 공백 없음
이름·부제에 든 단어는 자동 색인되므로 **중복 제외**(개발/어원/사전/프로그래밍/용어/유래/작명 등 제외).

| 안 | 값 | 글자수 |
|---|---|---|
| **A (권장)** | `코딩,컴퓨터,CS,IT,개발자,면접,취업,자료구조,알고리즘,깃,리액트,자바,파이썬,용어집,전공,주니어,영어,학습,기술,단어` | 68 |
| B | `코딩,컴퓨터공학,CS,IT,개발자,기술면접,취업,자료구조,알고리즘,주니어,비전공,용어집,영어단어,학습,깃허브,오픈소스` | 65 |

`[사람 결정]` — A는 폭넓은 기술 스택 단어(리액트/자바/파이썬)로 롱테일 검색 흡수, B는 타깃 의도어(기술면접/비전공/오픈소스)에 집중. 100자에 여유 있으니 최종 이름/부제 확정 후 빠진 핵심어를 채워 100자 가깝게 채우는 것을 권장(낭비된 글자는 손해).

### 프로모션 텍스트 (promotional text) — ≤ 170자
| 안 | 값 | 글자수 | 근거 |
|---|---|---|---|
| **A (권장)** | `'버그'는 왜 버그일까? 개발 용어의 어원과 작명 이유를 한국어로 풀어주는 사전. 650개 용어를 오프라인에서 즉시 찾고, 없는 용어는 AI가 설명합니다. 면접·학습용 북마크와 검색 기록까지.` | 107 | 후크(질문)+정의+기능 요약. 심사·과장 없음 |
| B | `외울 게 아니라 이해할 차례. 개발 용어가 왜 그 이름이 됐는지 어원부터 짚어주는 한국어 사전입니다. 650개 용어 오프라인 즉답, 없으면 AI가 채워줍니다.` | 88 | 가치 제안 중심(학습 동기). 더 짧고 담백 |

프로모션 텍스트는 심사 없이 수시 교체 가능 → 출시 후 시즌/기능 강조에 활용.

### 설명 (description) — ≤ 4000자 / 실측 744자
도입 3줄(접힘 전 노출)에 후크+정의가 들어가도록 배치.

```
'버그(bug)'는 왜 버그일까요? 진짜 나방 때문이었습니다.
개발 어원 사전은 매일 쓰는 개발 용어가 왜 그런 이름을 갖게 됐는지, 그 어원과 작명의 맥락을 한국어로 풀어주는 사전입니다.

단어의 철자만 외우는 공부는 금방 잊힙니다. '왜 이 이름인가'를 알면 개념이 오래 남습니다. 면접을 앞둔 주니어 개발자, CS를 배우는 전공·비전공 학생, 용어의 진짜 뜻이 궁금한 모든 개발자를 위해 만들었습니다.

[주요 기능]
• 어원·작명 관점 설명 — 단순 정의가 아니라 "왜 이렇게 부르게 됐는가"까지
• 650개 용어 오프라인 내장 — 네트워크 없이 즉시 검색, 빠른 자동완성
• 없는 용어는 AI로 — 내장 사전에 없으면 AI(Claude)가 그 자리에서 설명을 생성
• 북마크 — 면접·복습용으로 중요한 용어를 모아두기
• 검색 기록 — 찾아본 용어를 다시 빠르게

[이렇게 쓰세요]
• 면접 전, 헷갈리는 용어의 유래와 맥락을 빠르게 정리
• 문서·강의에서 처음 본 용어를 그 자리에서 검색
• 아는 용어도 '이름의 유래'로 다시 보며 개념 굳히기

[알아두실 점]
• 모든 설명은 AI가 생성하며 사람의 개별 검수를 거치지 않습니다. 부정확한 내용이 있을 수 있어 앱 내 제보 기능으로 알려주시면 개선합니다.
• AI 생성은 기기당 하루 횟수 제한이 있습니다. 내장 650개 용어는 제한 없이 오프라인으로 이용할 수 있습니다.
• 로그인이나 계정이 필요 없습니다.

문의: oddmuffinstudio@gmail.com
```

### 카테고리
| | 후보 | 근거 |
|---|---|---|
| 1차 (권장) | **교육 (Education)** | 학습·면접 준비 용도가 핵심 가치. 경쟁이 사전류보다 사용자 의도와 맞음 |
| 1차 대안 | 참고 (Reference) | "사전"이라는 형식에는 가장 정직한 매칭. 다만 발견성은 교육이 유리 |
| 2차 | **참고 (Reference)** 또는 개발자 도구 | 1차를 교육으로 두면 2차는 참고 권장 |

`[사람 결정]` — [ASO]·[사용자]는 교육 1차 / 참고 2차 권장(학습 의도 사용자에게 도달). 개발자 도구는 트래픽이 IDE·유틸 중심이라 발견성 낮음.

### 연령 등급
- **권장: 4+** (만 4세 이상). 폭력·성적 콘텐츠·도박·사용자 생성 노출 없음.
- `[사람 결정]` 주의: 설명이 **AI 생성**이고 외부에 무제한 노출되는 UGC가 아니라 정의된 용어 응답이므로 4+로 충분. 단 Apple이 "사용자가 임의 입력→AI 응답"을 웹/UGC로 해석할 여지가 있어, 설문에서 "제한 없는 웹 접근" 항목은 **아니오**로 답할 것(앱은 임의 웹브라우징 제공 안 함).

### 개인정보 라벨 (App Privacy) — 실제 수집과 반드시 일치
실제 수집(`site/privacy-policy.md` 기준): Firebase Analytics 이벤트 + 익명 App Instance ID. **명시적 동의(opt-in) 시에만 수집**. 계정/로그인 없음. 온디바이스 API 키 없음(백엔드 프록시 경유).

| 항목 | 답변 초안 |
|---|---|
| 데이터를 수집합니까? | **예** |
| 수집 데이터 유형 | **식별자 → 기기 ID**(Firebase App Instance ID, 익명) / **사용 데이터 → 제품 상호작용**(검색·오류 등 이벤트) |
| 사용 목적 | **분석(Analytics)** (앱 개선·사용 측정). 광고·트래킹 **아니오** |
| 사용자에 연결됩니까? | **아니오** (실명·이메일·계정과 연결 불가, 익명 식별자) |
| 사용자 추적(Tracking)에 사용? | **아니오** (타사 데이터와 결합·광고 추적 없음 → ATT 불필요) |
| 검색어 텍스트 | **확인됨 — 수집함.** `AnalyticsService.swift`의 `search`·`search_error` 이벤트가 `keyword` 파라미터로 검색어 원문을 Firebase에 전송(동의 시에만). → 라벨에 **"검색 기록(Search History)" 유형 추가 필수** (또는 "사용 데이터"로 분류하되 검색어 전송 사실 반영) |

`[사람 결정]` — opt-in 동의 UI가 라벨의 "수집"과 모순되지 않게(동의 안 하면 미수집임을 라벨 설명에 반영 가능). **검색어 전송은 코드로 확인 완료**(`logSearch/logError`의 `keyword` 파라미터) → 라벨에서 검색어 수집을 누락하면 라벨-실제 불일치(리젝/정책 위반 리스크)이므로 반드시 포함.

### 스크린샷 계획 (이미지는 사람, 카피·구성만)
| 컷 | 화면 | 캡션(한 줄) | 메시지 |
|---|---|---|---|
| 1 | 검색 결과(예: bug) | **"왜 버그일까? 어원부터 알려줘요"** | 후크·핵심 차별점 |
| 2 | 자동완성/검색창 | **"650개 용어, 오프라인에서 즉시"** | 규모·오프라인 속도 |
| 3 | 상세(어원·작명 이유) | **"정의가 아니라 '이름의 이유'까지"** | 깊이 있는 설명 |
| 4 | AI 생성 결과 | **"없는 용어는 AI가 그 자리에서"** | 커버리지 확장 |
| 5 | 북마크/검색 기록 | **"면접·복습용으로 모아두기"** | 타깃 유스케이스 |

`[사람]` — 6.7"·6.5"(또는 현행 필수 사이즈) 캡처 필요. 1번 컷이 첫인상이므로 가장 공들일 것. 다크모드 컷 1장 섞으면 완성도 인상↑.

### 마케팅 / 지원 URL
- **지원 URL `[사람 결정]`**: 필수. 후보 ① 개인정보처리방침과 같은 호스팅의 간단한 지원 페이지(`site/` 정적 페이지 활용) ② 그조차 어려우면 `mailto:` 안내가 담긴 단일 페이지. App Store는 실제 도달 가능한 페이지를 요구 → 빈 페이지/깨진 링크 금지.
- **마케팅 URL(선택)**: 랜딩 없으면 생략 가능. 지원 URL 우선 확보.
- 개인정보처리방침 URL: `site/privacy-policy.md`의 호스팅 주소를 App Privacy 섹션에 입력 `[사람]`.

---

## 심사 리스크 체크리스트
- [ ] **개인정보 라벨 ↔ 실제 수집 일치** — 특히 검색 키워드 전송 여부 코드 확인 후 라벨 확정 (최우선)
- [ ] **AI 생성 콘텐츠 고지** — 설명 본문 + 온보딩 1회 고지 + 제보 경로 존재(앱 내). UGC/부정확성 관련 1.2/4.0 리스크 완화
- [ ] **연령 등급 설문** — "무제한 웹 접근" 아니오, AI 응답은 정의된 사전 응답임을 일관되게
- [ ] **과장 표현 없음** — "최고/1위/완벽" 미사용 확인(현재 초안 클린)
- [ ] **플랫폼·타사명 오용 없음** — Claude/AI는 기능 설명 맥락에서만 사용, 비교·비방 없음
- [ ] **지원 URL 실제 도달 가능** — 빈/깨진 링크 금지
- [ ] **ITSAppUsesNonExemptEncryption** 선언(HTTPS만 → 보통 면제) — launch-prep C 항목
- [ ] **메타데이터 내 연락처 일치** — 설명·지원의 이메일 = `oddmuffinstudio@gmail.com`

## 사람이 직접 해야 하는 일
- [ ] 이름/부제/키워드 **최종 조합 선택**(위 권장: 이름 A + 부제 A, 키워드는 확정 후 100자 채우기)
- [ ] **검색 키워드 Analytics 전송 여부 코드 확인** → 개인정보 라벨 확정
- [ ] 스크린샷 5컷 촬영(필수 사이즈, 다크모드 1장 권장) + 앱 아이콘 최종
- [ ] 연령 등급 설문 제출 / 개인정보 라벨 App Store Connect 입력
- [ ] 지원 URL(+개인정보처리방침 URL) 호스팅·입력
- [ ] Distribution 서명·아카이브·Export compliance 선언 (launch-prep C)
- [ ] 최종 결정/입력 완료 후 **`ROADMAP.md` `[Ops] 출시 준비` B 항목 상태 갱신**
